# Unbranded Resource Load (whitelabel)

This folder holds the **local** whitelabel `.pbiviz` after `npm run package:whitelabel`.

The package is **gitignored** so the certification / branded repo does not ship a second GUID artifact.

```bash
npm run package:whitelabel
# → downloads/wl/ResourceLoad.pbiviz
```

GUID: `resourceLoadWL848D396D0B695DE6F58CE0081268A861` (distinct from branded).

See [`docs/WHITELABEL.md`](../../docs/WHITELABEL.md).
